apiVersion: v1
kind: Service
metadata:
  name: products-svc
  labels:
    app.kubernetes.io/name: products
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: products
    